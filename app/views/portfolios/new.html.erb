<%= form_for @portfolio do |form| %>

<%= form.label :title %>
<%= form.text_field :title %><br/>

<%= form.label :body %>
<%= form.text_area :body %><br/>
<%= form.label :image %>
<%= form.file_field :image %><br/>

<%= form.submit 'submit'%>

<%end%>	